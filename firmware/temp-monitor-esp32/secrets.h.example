#pragma once

// Copy this file to secrets.h and set FIREBASE_DEVICE_KEY to match RTDB Rules.
// secrets.h is gitignored — never commit the device key.

#define FIREBASE_DB_HOST "temperaturedashboard-c8883-default-rtdb.asia-southeast1.firebasedatabase.app"
#define FIREBASE_DEVICE_ID "esp32-s3-kls"
#define FIREBASE_DEVICE_KEY "REPLACE_WITH_YOUR_DEVICE_WRITE_SECRET"
